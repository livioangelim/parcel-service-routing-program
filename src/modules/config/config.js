export const config = {
    api: {
        baseUrl: '/api',
        endpoints: {
            route: '/route'
        }
    },
    validation: {
        weight: {
            min: 0.1,
            max: 1000
        }
    }
};

export default config;
